version: 2
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

# 必须填这个，解决报错！！
sphinx:
  configuration: docs/conf.py

# 把你的所有HTML文件发布出去
jobs:
  pre_build:
    - mkdir -p docs/
    - "echo 'project = \"test\"' > docs/conf.py"
  post_build:
    - cp -r ./* $READTHEDOCS_OUTPUT/html/
