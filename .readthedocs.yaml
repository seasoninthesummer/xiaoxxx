version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_checkout:
      - mkdir -p docs
      - echo 'project="static"' > docs/conf.py
    post_build:
      - cp -r ./* $READTHEDOCS_OUTPUT/html/

sphinx:
  configuration: docs/conf.py
