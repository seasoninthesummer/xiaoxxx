version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

# 发布静态 HTML（你的网页文件）
sphinx:
  configuration: null

# 把你的 HTML 复制到发布目录
jobs:
  post_build:
    - mkdir -p $READTHEDOCS_OUTPUT/html/
    - cp -r ./* $READTHEDOCS_OUTPUT/html/
